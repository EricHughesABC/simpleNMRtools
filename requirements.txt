# ── conda-managed packages ──────────────────────────────────────────────────
# The following are installed via conda in Step 3 of the README.
# They are listed here for reference only — do NOT install via pip
# as the conda builds are required for correct binary linking:
#
#   conda install conda-forge::openjdk
#   conda install -c conda-forge rdkit numpy scipy pandas networkx pillow
#
#   rdkit==2024.3.3
#   numpy==1.26.4
#   scipy==1.14.0
#   pandas==2.2.2
#   networkx==3.3
#   pillow==10.4.0

# ── pip-managed packages ─────────────────────────────────────────────────────

# Flask web framework and extensions
Flask==3.0.3
Flask-SQLAlchemy==3.1.1
Flask-Migrate==4.0.7
blinker==1.8.2
click==8.1.7
itsdangerous==2.2.0
Werkzeug==3.0.3

# Templating
Jinja2==3.1.4
MarkupSafe==2.1.5

# Database drivers
PyMySQL==1.1.1

# Environment / configuration
python-dotenv==1.0.1

# Logging
loguru==0.7.2

# Java bridge (for NMRShiftDB predictions)
jpype1==1.6.0

# Date/time utilities
python-dateutil==2.9.0.post0
pytz==2024.1
six==1.16.0
tzdata==2024.1

# Image processing (also installed via conda — pip install is a no-op if versions match)
# pillow==10.4.0

# Documentation (development only — not needed on PythonAnywhere)
# sphinx
# sphinx-rtd-theme
# pandoc
